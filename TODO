- add menus to search
- filter search on rating
- show vegan/veg. icon

- add ACL to library
- create method to share a library

- build a "starter" library when newly created
- fix bug where tags aren't indexed for search during restore
